exports.pricesToArray = prices => {
    // const arrPrices = [];
    // Object.keys(prices)
    //     .forEach(p => {
    //         let values = prices[p].forEach(o =>{
    //             return sum + o.value;
    //         }, 0);
    //         // buy.push({price: p, values});
    //     });
    
};